# Linting & Formatting

- Follow the project's ESLint (flat config) and Prettier configs.
- Run `yarn lint` and `yarn format` before committing.
- Don't disable lint rules without a comment explaining why.
- Import sorting is handled by `@trivago/prettier-plugin-sort-imports`.
